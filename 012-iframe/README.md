# HTML 012：內嵌框架標籤 (iframe)

## 內容
- **`<iframe>` 標籤**：用於在目前的網頁中，嵌入另一個獨立的網頁或多媒體資源。
- **常用屬性**：
  - `src`：內嵌網頁的網址來源。
  - `width` / `height`：控制這個內嵌視窗的寬度與高度（可用像素 px）。
  - `style="border:0;"`：用來去除內嵌視窗原本醜醜的邊框。
  - `allowfullscreen`：允許使用者點擊「全螢幕」播放。