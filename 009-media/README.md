# HTML 009：多媒體標籤

## 內容
- **圖片標籤 `<img>`**：用於在網頁中嵌入圖片。必須設定 `src`（來源路徑）與 `alt`
- **音訊標籤 `<audio>`**：用於嵌入音樂。加上 `controls` 屬性才能在網頁上顯示播放面板。
- **影片標籤 `<video>`**：用於嵌入影片。透過 `width`、`height` 控制畫面大小，同樣需要 `controls` 屬性才能播放。