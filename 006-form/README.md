# HTML 006：表單應用

## 內容
- **表單容器 `<form>`**：定義資料傳輸的範圍。
- **標籤 `<label>`**：為輸入框設定說明文字，提升親和力。
- **輸入框 `<input>`**：
  - `type="text"`：單行文字輸入。
  - `type="password"`：隱藏輸入內容的密碼框。
- **按鈕 `<button>`**：用於觸發提交動作。